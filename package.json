{
  "name": "quantum-etz-chaim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "vendor/ivritcode/packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build:shared": "npm run build --workspace @ivritcode/core && npm run build --workspace @qec/core",
    "build": "npm run build:shared && tsc --noEmit && vite build && node scripts/verify-published-links.mjs",
    "format:check": "prettier --check .",
    "test": "npm run build:shared && vitest run tests",
    "typecheck": "npm run build:shared && tsc --noEmit"
  },
  "dependencies": {
    "@ivritcode/core": "1.0.0",
    "@qec/core": "0.1.0",
    "@qec/spec": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "ajv": "^8.20.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
